# Manfred handoff — 2026-09-06 UTC / September 5 MDT

Work paused at the user's request because Codex usage is nearly exhausted. This document distinguishes tested delivery from unfinished work; do not restart hardware tests or resend held images without reading it.

## Tracking issues

- [Trigger an instant BLE preview from the physical EyeVue shutter](https://github.com/thetopham/manfred/issues/6)
- [Fix ChatGPT Live photo confirmation and safely recover the held Tasker queue](https://github.com/thetopham/manfred/issues/7)
- [Investigate higher-resolution BLE photos using matching TK80201 firmware offline](https://github.com/thetopham/manfred/issues/8)
- [Existing Wi-Fi latency issue#5](https://github.com/thetopham/manfred/issues/5)

## Goal and immediate priorities

Wear EyeVue glasses, press a glasses/phone/app button, receive an image quickly, then have Tasker open/use ChatGPT Live and attach that exact image. Bluetooth speakers/mic already work. Wi-Fi originals are high quality but AP discovery is intermittent. The user now wants instant BLE previews as an alternative, physical-button triggering, and offline investigation of higher-resolution BLE firmware.

1. Recover the already-sent held Tasker record safely, then fix focused-Live confirmation.
2. Implement and verify physical-button BLE triggering; it is NOT implemented in0.5.4.
3. Continue exact-model firmware analysis offline; no flash is authorized by this handoff.
4. Keep Wi-Fi latency work under existing issue#5; retain full-resolution mode.

## Repository and installed build

- Repository: https://github.com/thetopham/manfred ; working branch feat/eyevue-fast-discovery-tasker; draft PR#4: https://github.com/thetopham/manfred/pull/4 . Main/PR#3 already merged earlier. PR#4 is not merged.
- Checkout on SSH host demerzel: /home/matt/workspace/demerzel-extraction-20260905/manfred-eyevue . Windows uses WSL Ubuntu-24.04 and its saved SSH alias.
- Installed on S25: **Manfred0.5.4+12**, app source **1c53042f9377c502c40305837bc0be402d07ff9d**. This replaces0.5.3+11, preserving settings. Stable signing belongs to Manfred; do not migrate secrets back to demerzel-idle-compute.
- Signed build: https://github.com/thetopham/manfred/actions/runs/34008460214 . Analysis clean; **64 Flutter +107 native tests passed**, native failures/errors/skips0. APK SHA256 **fa78dffc397738ea1b3908ce1b56f8e14b6201e48a5f9bf2e781de3149a522c6**; source receipt and hash verified; adb install-r succeeded and package version12/0.5.4 confirmed.
- APK local: Desktop/manfred-resume-2026-09-05/Manfred-0.5.4-EyeVue.apk . Remote build/logs: /home/matt/artifacts/manfred-owned-build-20260906/1c53042/ . GitHub artifact has14-day retention; local copy remains.

## Implemented and measured

The app remembers **Instant BLE preview ·320x180** versus **Wi-Fi original ·full resolution**. Wi-Fi remains the default for existing installations. BLE uses Start BLE preview then **Take preview in the app**. Source changes are disabled while a session owns the foreground lease. BLE requires no AP connection or Wi-Fi permissions and does not apply the Wi-Fi20% battery gate. Existing Wi-Fi capture/media/live options remain available.

Native BLE subscribes before0x22[31], assembles the complete AA15 JPEG, permits one capture at a time, drains an accepted write on Stop, and uses the same validated JPEG/MediaStore/hash/immutable-ID/Tasker receipt pipeline. Optional imageSource distinguishes BLE from Wi-Fi originals. It does not resize the image. No physical-button listener was added.

On this TK8/0201 (BT1.1.9, ISP3.3.7, device2):

| Capture | Dimensions/bytes | Request to received | Request to saved | Outcome |
|---|---|---|---|---|
| Supervised app BLE test |320x180 /11,658|3.315s|**3.381s**|Queued, Voice opened, Send attempted; exact image independently visible in ChatGPT Live. Worker held due to missing automatic confirmation.|
| Subsequent app BLE capture in same session |320x180 /9,942|3.197s|**3.250s**|Saved and queued behind held first receipt; not claimed as sent.|

First capture kept the same phone Wi-Fi SSID; no glasses media/live AP startup commands were observed. The timings above end at phone save, not full ChatGPT delivery. The first Tasker attach/send observation lasted16.846s and ended ambiguous. Uninterrupted audio/server receipt were not verified.

## Critical Tasker state — avoid duplicate sends

Installed production projects are Manfred_Photo_Queue and Manfred_ChatGPT_Worker. Existing Base/CyanBridgeAI remain. Tasker files under /sdcard/Tasker/Manfred are unchanged by0.5.4: eyevue-ui-worker.java.txt SHA256fb46dc580d5577cf276053be69028f0182dbb36b06b6b1a6e59a3dc9dd3e2652 and receipt.js SHA256b68d2d9e19d76e2840ae865f791108318784a1e423b25b45204a2353f5a89cb5.

The first BLE receipt is **held** with submission_observed_confirmation_unverified. A fresh screenshot was compared directly with its actual cached JPEG and showed that exact image in Live. **Do not retry it.** Held blocks new claims; the subsequent preview is queued. Current exact ID/owner and private evidence are local in ble-054-acceptance/first-ui-worker.json, handoff-worker.json and handoff-receipt.json. Re-read current queue before any recovery.

The focused Live view can show a small orb with a floating photo and no accessible Image node. The worker wrongly inferred transcript_small_orb from width309/window1080, despite zero transcript controls. Its post-send normalization only handles focused_large_orb. UIAutomator failed to reach idle state during this animation; never reuse a stale dump. The screenshot independently proved display, not the automatic checker.

The repository's guarded Manfred Replay Resolve helper (task52; verify whether installed) supports exact-ID/owner sent resolution after independent review and stopped Worker/Dispatch, checks the unchanged snapshot and does not dispatch. See the dedicated issue. A diagnostic-only small-orb->unknown patch was started, then frozen: it is **untested, undeployed, retained outside Git**, and does not fix normalization. Its path is /home/matt/artifacts/manfred-owned-build-20260906/tasker-small-orb-diagnostic-unverified.patch. App and installed worker remain at their tested source.

Previous Tasker controlled-replay evidence remains:55 Node,27 Python generator/recovery,46 Java runtime tests plus BeanShell parse passed; all13 installed worker/dispatcher actions matched the portable bundle. Two saved-original controlled submissions took3.312/3.777s during attachment stages only. These do not prove reliability for the newly observed focused view.

## Firmware and hardware

See [hardware profile](hardware/eyevue-e09.md) for pinned primary sources, protocol commands, prior probes and OTA variants. Complete0x22[31] transfers already return320x180; packet MTU cannot add JPEG pixels. The reverse repo does not demonstrate a larger BLE JPEG;0x32/33 photo probes are speculative. Physical shutter gives a stored original without a proven AA15 preview; requesting0x31 afterward would be another exposure and needs loop/duplicate guards.

The exact TK80201 BLE1.1.9 package was downloaded and verified (1,809,504 bytes, SHA25643d6a5011e18b192cde698aa90019565298e176d257b355a052eff7b277bd57a). Both live catalog checks returned no Wi-Fi/ISP package. Loader strings/directory were identified offline, but no camera-resolution patch. Main/T AP OTA is raw TCP5007; other-model HTTP instructions must not be applied to this unit. Device-side signatures/encryption/secure boot/recovery remain unverified. Nothing was executed or flashed.

Offline report and package: /home/matt/artifacts/eyevue-firmware-offline-20260906T032619Z/ and Desktop/manfred-resume-2026-09-05/firmware-offline-20260906/ . Do not commit binaries or private catalog/test artifacts.

## Cleanup, evidence and resume

Manfred was force-stopped to end the test after the screen had changed; the installed app and settings remain. The focused log collector was stopped. Original phone settings were restored and read back: screen_off_timeout30000, stay_on_while_plugged_in0. No test collector remains active. ChatGPT was not force-stopped; its Live state may change independently. No held record was resolved and no automatic retry was performed.

Local evidence root: Desktop/manfred-resume-2026-09-05/ble-054-acceptance/ . Contains settings backup (PRIVATE), first-acceptance.json, screenshots/JPEG evidence (PRIVATE), Tasker result files (PRIVATE), focused log, collector.json(activefalse), temporary-settings.json(restoration_pendingfalse). Earlier0.5.3 test report, portable Tasker bundle and latency logs remain in the parent directory. Keep raw logs/photos/queue exports/credentials out of public GitHub.

For future ADB, use Downloads/platform-tools-latest-windows/platform-tools/adb.exe. The phone often disconnects or relocks; verify devices and fresh lock/UI state first. phone-ui-helper.ps1 fails closed on dump errors. Preserve existing user projects and settings. Stable CI build artifacts can replace the installed app; PR ephemeral APKs cannot be used for that replacement.

Broader earlier review topics (Manfred ears, Demerzel idle compute, brain ingestion, fleet, Hermes versus GPT backend) were not re-audited during this BLE/firmware work. Do not infer architectural decisions from this handoff. Manfred app source/build ownership is in the Manfred repository; CyanBridge remains protocol/prototype evidence.
