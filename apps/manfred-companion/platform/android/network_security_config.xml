<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Omi uploads validate tailnet endpoints at runtime. EyeVue HTTP is limited
         to its fixed local AP address and sockets on the selected glasses network. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
